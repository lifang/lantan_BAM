<%= stylesheet_link_tag    "application", "style"%>
<%= javascript_include_tag "application", "main","jquery.tablesorter.min"%>
<script>
    $(document).ready(function(){

        $("#print_sort_table").tablesorter({
        headers:
        {
            0: {
                sorter: false
            },
            3: {
                sorter: false
            },
            4: {
                sorter: false
            },
            5: {
                sorter: false
            },
            6: {
                sorter: false
            },
            7: {
                sorter: false
            }
        }
    });
    });

</script>
<table width="100%" border="0" cellspacing="0" cellpadding="0" class="data_table" id="print_sort_table">
  <thead>
  <tr class="hbg">
    <th>条形码</th>
    <th><span class="sort_u">物料名称</span></th>
    <th><span class="sort_d">物料类别</span></th>
    <th>库存状态</th>
    <th>库存量（个）</th>
    <th>成本价（元）</th>
    <th>盘点实数</th>
  </tr>
  </thead>
  <tbody>
  <% (@materials_storages || []).each do |material| %>
      <tr>
        <td><%= material.code %></td>
        <td><%= material.name %></td>
        <td><%= Material::TYPES_NAMES[material.types.to_i] %></td>
        <td><%= material.storage.to_i > 0 ? "存货" : "缺货" %></td>
        <td><%= material.storage.to_i %></td>
        <td><%= material.price %></td>
        <td><%= material.check_num.to_i %></td>
      </tr>
  <% end %>
  </tbody>
</table>
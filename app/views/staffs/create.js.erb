<% if @flash_notice == "success" %>
  window.location.replace("<%= store_staffs_path(@store) %>");
<% else %>
  tishi_alert("<%= @flash_notice %>");
  $("#new_staff_btn").removeAttr("disabled");
<% end %>
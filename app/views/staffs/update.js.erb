<% if @flash_notice == "success" %>
  window.location.replace("<%= store_staff_path(@store, @staff) %>");
<% else %>
  tishi_alert("<%= @flash_notice %>");
  $("#edit_staff_btn").removeAttr("disabled");
<% end %>
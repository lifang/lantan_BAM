<table width="100%" border="0" cellspacing="0" cellpadding="0" class="data_table">
  <thead>
    <tr class="hbg">
      <td>投诉时间</td>
      <td>投诉理由</td>
      <td>投诉要求</td>
      <td>责任人</td>
      <td>相关订单</td>
      <td>处理结果</td>
      <td>操作</td>
    </tr>
  </thead>
  <tbody>
    <% @complaints.each do |complaint| %>
    <tr>
      <td><%= complaint.created_at.strftime("%Y-%m-%d %H:%M:%S") if complaint.created_at %></td>
      <td><%= complaint.reason %></td>
      <td><%= complaint.suggstion %></td>
      <td><%= complaint.st_name1 %>，<%= complaint.st_name2 %></td>
      <td><a href="#" class="other_a">1558755587</a></td>
      <td><%= complaint.remark %></td>
      <td><a href="#">处理</a></td>
    </tr>
    <% end %>
  </tbody>
</table>
<%=  will_paginate_remote(@complaints , "",
  {:url => "/stores/#{@store.id}/customers/#{@customer.id}/complaints"}) %>